server:
  port: 9999

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ashwanikumarshaw/ROM-Config-Server
          clone-on-start: true
          skipSslValidation: true